{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'd=$(for p in \"$HOME/.claude/plugins/cache\"/*/*/*/skills/onboard-plugins/scripts; do [ -f \"$p/check-updates.py\" ] && echo \"$p\" && break; done); [ -n \"$d\" ] && python3 \"$d/check-updates.py\" --quiet'"
          }
        ]
      }
    ]
  }
}
